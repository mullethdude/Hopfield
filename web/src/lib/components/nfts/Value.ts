import {get, writable} from 'svelte/store';

export const tokentransient = writable(0);
export const colorscheme = writable(0);
export const px = writable(0);
export const py = writable(0);
export const situ = writable([]);